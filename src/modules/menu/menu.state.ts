export const menuInitialState = {
  open: false,
  selected: null,
};

export enum menuActions {
  "OPEN_MENU",
  "CLOSE_MENU",
  "SET_SELECTED",
  "INIT",
}

const menuReducer = (state = menuInitialState, action: any) => {

     switch(action.type){
           
     }
};
