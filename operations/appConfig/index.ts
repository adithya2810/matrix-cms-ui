import { GET_NAV_MENU_STATE } from "./appConfig.queries";
import appConfigMutation from "./appConfig.mutation";

const appConfiqQuery = {
  GET_NAV_MENU_STATE,
};

export { appConfigMutation, appConfiqQuery };
